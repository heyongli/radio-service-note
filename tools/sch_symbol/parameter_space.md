# tools/sch_symbol/parameter_space.md — 参数调优空间学习记录

记录 sch_symbol 系列识别程序的**参数调优规律与实测轨迹** (每次调参留档,
防"调好了但不知道为什么")。算法原理见 readme.md, 这里是参数空间。

## 调参规律

1. **先参数化再调优**: 阈值全走 CLI (architecture §5.9), 否则无法系统扫描。
2. **评估闭环先行**: 调参必须有 GT + 量化指标 (如 hit<50px), 否则无法判断好坏。
3. **约束窗口避开被测目标本身** (关键教训):
   - C145: 板间空检查 band 含板线 → 暗占比 24% 被误杀, 实为合法电容
   - 修正: band 只取两板内侧中央区, 板线外侧单独做接线探测
4. **多源投票 > 单源优先** (各源有盲区):
   - plates 海量假候选 (C269 997个); find_cap_pairs 方向可能误报 (C145)
   - 三源 (板线对/极板/走线断口) 一致性 = 高置信
5. **symbol_pos 本身会漂移**: C269 偏移 179px 超半径 → 需结合绿线触点/尺寸库反查。
6. **调参策略**: 固定算法 → 单参数扫描 → 对照 GT 看指标曲线 → 找平衡点。

## sch_cap 参数空间 (全 CLI)

| 参数 | 含义 | 调优方向 |
|---|---|---|
| `--gap-empty-th`/`--gap-empty-frac` | 板间空检查 | 过严漏检, 过松误检 |
| `--wire-px`/`--wire-pen` | 接线约束权重 | 引线是否必须存在 |
| `--touch-pen`/`--touch-r` | 绿线触点 | 是否强制在 flow 上 |
| `--vote-tol`/`--vote-bonus` | 多源投票 | 越大越信投票 |
| `--radius` | 搜索半径 | 补偿 symbol_pos 漂移 |

## 实测轨迹

### 2026-09-17 round029 基准
- 算法: 单源优先 (plates → pairs → 断口)
- 指标: **8/14 hit** (<50px) 对照 gt_rx_flow.json
- 主要 miss: C77/C243 (检测错位), C269 (symbol_pos 漂移 179px)

### 2026-09-17 多源投票 v1 (未调参)
- 算法: find_cap_pairs 主源 + plates/断口投票, 质量分选优
- 指标: **6/14 hit** (退化, 因 cap_quality 约束误杀 C145/C152/C136/C244)
- 教训: 板间空检查 band 含板线本体 → 真电容被拒; 已修正 in_gap 窗口

### 2026-09-17 修正轮 (3 个真 bug)
- **bug1 板线 cy 偏差**: _plate_center 选"最粗暗段"→ 选了走线段 (17px) 而非
  板线段 (12px); 改 span=40 + 选**最短**板线暗段 → cy 修正到真板线中心
  (C145 单独测 d_gt 30→2)
- **bug2 板间空误杀**: cap_quality 板间空在 gray 上查, 绿线穿过电容算"非空";
  改为在 **pb (去绿线) 上查** → 真电容不被误杀
- **bug3 方向硬编码**: detect_plates_on_wire 竖板线 → dir 应 "h" (走线水平),
  原写死 "v" → cap_quality 用错方向采样
- 指标: 7→**8/14 hit** (稳定基线)
- 结论: cap_quality 距离惩罚主导, 走线交叉处"看起来像电容"; 需 **net 判据**
  (真电容两侧 net 不同, 板线为桥) 才能真正区分 → 独立任务

---
调参时必须更新此表: 参数快照 / 指标 / 结论。